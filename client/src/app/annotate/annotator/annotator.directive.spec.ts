import { AnnotatorDirective } from './annotator.directive';

describe('AnnotatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AnnotatorDirective();
    expect(directive).toBeTruthy();
  });
});
